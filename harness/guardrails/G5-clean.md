# 护栏5 · 代码清洁（Clean Code Gate）

## 规则
- 不生成死代码、垃圾代码、冗余注释。
- 提交前清理：删除无用代码、未使用的变量/函数/导入、大段过期注释。
- 保持代码干净，避免几轮累积后后续 AI 越来越难迭代。

## 检查点
- 有无定义了但没调用的函数/变量？
- 有无复制粘贴后没改干净的残留？
- 注释是否与代码一致？是否一堆「临时」注释没删？
- 有无被整段注释掉的旧代码？

## 禁止
- 禁止「先跑通再说，注释以后删」（以后不会删）。
- 禁止留大段被注释掉的旧代码。
- 禁止为凑数写用不上的工具函数。
