# 版本快照（护栏4）

用法：
- 你觉得「这一轮稳了」时，手动存一次盘：`snapshots/<版本号>/` 保存当前可运行版本。
- 上轮写炸了 → 触发回退到上一版本快照，在干净基线重来。
- 原版本不删，可随时切回。

建议版本号：v1, v2 ... 或用日期时间（如 20260802-1812）。
harness 验证稳定前保持手动；以后可升级自动。

> 回退由用户主动触发；AI 不擅自回退，也不擅自删除快照。
